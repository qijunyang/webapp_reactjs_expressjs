{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/App.jsx --bundle --outfile=dist/app.js --format=esm --jsx=automatic --external:react --external:react-dom"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "esbuild": "0.25.5"
  }
}
